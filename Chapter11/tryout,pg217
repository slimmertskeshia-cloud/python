
#pytest test_cities.py
#  city_functions.py

def city_country(city, country, population=None):
    """Return a string in the form 'City, Country' or 'City, Country – population xxx'."""
    if population:
        return f"{city.title()}, {country.title()} – population {population}"
    else:
        return f"{city.title()}, {country.title()}"

# test_cities.py
def test_city_country():
    result = city_country('santiago', 'chile')
    assert result == 'Santiago, Chile'

def test_city_country_population():
    result = city_country('santiago', 'chile', population=5000000)
    assert result == 'Santiago, Chile – population 5000000'
# To run the tests, use the command line:

